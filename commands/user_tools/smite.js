const { EmbedBuilder } = require('discord.js');

module.exports = {
    name: 'smite',
    description: 'Ping command',
    execute(message, args) {
        // Calculate latenc

        message.channel.send("<@1228254454483124226> le smite");
    },
};
